/target
**/*.rs.bk
*.so
*.dylib
*.dll
*.egg-info/
__pycache__/
.venv/
dist/
*.whl
node_modules/
*.node
crawlee-storage-node/artifacts/
artifacts/

# Generated by `napi build`, which bakes the package version into it — so it can only be produced
# once the version is final. The release workflow ships it from a build artifact. index.d.ts has no
# version in it and stays tracked, so API surface changes still show up in diffs.
/crawlee-storage-node/index.js
